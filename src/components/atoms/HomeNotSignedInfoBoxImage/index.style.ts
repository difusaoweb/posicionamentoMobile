import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  image: {
    height: '100%',
    width: '100%',
    maxHeight: 200,
    maxWidth: '100%',
    minHeight: 8,
    minWidth: 1,
    opacity: 0.9
  }
})
